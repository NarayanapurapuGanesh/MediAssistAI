name: mobile
description: "MediAssistAI - Intelligent Health Monitoring and Medicine Reminders"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0
  shared_preferences: ^2.5.5
  provider: ^6.1.5+1
  google_fonts: ^8.2.1
  fl_chart: ^1.2.0
  intl: ^0.20.3
  flutter_local_notifications: ^22.3.0
  timezone: ^0.11.1
  shimmer: ^3.0.0
  flutter_timezone: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/
